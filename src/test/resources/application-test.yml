spring:
  application:
    base-url: localhost
  datasource:
    url: # dynamic
    username: # dynamic
    password: # dynamic
  jpa:
    hibernate:
      ddl-auto: create-drop
  mail:
    host: # dynamic
    port: # dynamic
    properties:
      mail.smtp.auth: false
      mail.smtp.starttls.enable: false

sgl:
  alert:
    environment: TEST
