spring:
  application:
    environment: local
    base-url: http://localhost:8080/api
    public-base-url: http://localhost:8080/api
  datasource:
    url: jdbc:mariadb://localhost:3308/application
    username: user
    password: secret
  jpa:
    hibernate:
      ddl-auto: create-drop
  mail:
    host: localhost
    port: 1025
    alert:
      enabled: false
    properties:
      mail.smtp.auth: false
      mail.smtp.starttls.enable: false

# unstable and on demand
#rest:
#  ga:
#    url: https://ga-staging.scoutsengidsenvlaanderen.be/groepsadmin/rest-ga

keycloak:
  resource: master-realm
  realm: master
  auth-server-url: http://localhost:8090/

logging:
  level:
    root: INFO
    be.sgl.backend: TRACE

organization:
  api-key:
    # Leave mollie blank to enable LocalCheckoutProvider for local testing
    # To use real Mollie in local dev, uncomment and set your test API key:
    # mollie: "test_xxxxxxxxxxxxxxxxxx"
    mollie:

